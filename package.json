{
  "name": "pocket-kingdom",
  "version": "2.0.0",
  "private": true,
  "homepage": ".",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate-data": "node scripts/generate-data.mjs",
    "deploy": "vite build && echo 'Build complete. Deploy dist/ folder.'"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
